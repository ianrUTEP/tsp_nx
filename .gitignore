#shared cache folder for library, not needed
__pycache__
#generated by HTML libraries
lib
#virtual environments
venvwin
venvwintwo
#These are generated often, not all of them are worth keeping track of
graphvisuals
#VSCODE workspace, not needed by others
tsp_nx_win.code-workspace
#log files shouldn't be gotten by git
logs
#too many pickles for when I dump sessions, don't need git to keep track of them
pickles